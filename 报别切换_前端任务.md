# 筛选页报别切换 —— 前端实施任务

**仓库分支基线**：`codex/submission-archive`
**本任务只做前端。后端由另一位实施者负责，接口行为已按第三节的契约实现完毕。**

---

## 零、路径白名单

**允许修改：**

```
src/console/web_templates/manual_filter.html
src/console/web_static/js/manual_filter/     （该目录下的模块）
src/console/web_static/css/modules/filter.css
```

**严禁修改：**

```
任何 .py 文件
```

后端接口已经就绪。如果你认为某个行为必须改后端才能实现，**不要动手，在交付说明里写出来**。

另外注意：`/duty` 路径复用的是同一个 `manual_filter.html` 模板（传入 `workspace_mode="duty"`），不是 `duty.html`。改动模板时请确认不会破坏值班编辑视图。

---

## 一、要实现什么

筛选页的"采纳"和"备选"按钮，目前一律把新闻归到**综报**。现在要在页面上加一个**综报 / 晚报**切换控件，让操作者自己决定这一条采纳后进哪个报。

切换控件影响的范围**仅限于**：

- "采纳" / "备选" 两个动作的归属
- 采纳列表 / 备选列表的展示内容
- 计数徽标中采纳 / 备选两个数字

**不影响**候选列表（待筛选）和丢弃列表——这两个是共用池子，无论切到哪个报别，内容完全一样。

---

## 二、核心概念

`report_type`（报别）只对"已经归了报"的新闻有意义：

| 状态 | 报别有意义吗 |
|---|---|
| 待筛选 | **没有**——还没归报 |
| 采纳 | **有** |
| 备选 | **有** |
| 已丢弃 | **没有**——跟原本打算发哪个报无关 |

所以：**待筛选和已丢弃是共用池子，采纳和备选按报别区分。** 界面行为都从这一条推出来。

---

## 三、API 契约

后端已按下表实现。**请严格照此发送请求。**

| 端点 | 是否发送 `report_type` |
|---|---|
| `GET /api/manual_filter/candidates` | **不发送** |
| `GET /api/manual_filter/discarded` | **不发送** |
| `GET /api/manual_filter/review` | **发送当前切换值** |
| `GET /api/manual_filter/stats` | **发送当前切换值** |
| `POST /api/manual_filter/decide` | **发送当前切换值** |
| `POST /api/manual_filter/order` | **发送当前切换值** |
| `POST /api/manual_filter/edit` | **不发送** |
| `POST /api/manual_filter/archive` | **不发送** |
| `POST /api/manual_filter/duplicate-check` | 发送当前切换值（现有行为不变） |

这些端点的参数/字段**本来就已经存在**（默认 `"zongbao"`），你不需要新增任何接口或字段，只需要在正确的请求上带上正确的值。

标记"不发送"的四个端点，后端会忽略该参数。但请仍然不要发送——**发送一个会被忽略的值，会让后面读代码的人误以为它有作用。**

### `POST /decide` 的一个细节

这个端点一次请求可以同时带四组 id：`selected_ids` / `backup_ids` / `discarded_ids` / `pending_ids`，但只有一个 `report_type` 字段。

后端会自动只把该值应用到 `selected_ids` 和 `backup_ids`，对 `discarded_ids` 和 `pending_ids` 保持原值。**所以你正常发送当前切换值即可，不需要为"这批里混了退回待定的条目"做任何特殊处理。**

---

## 四、界面行为要求

### 4.1 切换控件

放在筛选页的显眼位置，两个互斥选项：综报 / 晚报。当前选中状态必须**一眼可辨**。

**默认值固定为"综报"，每次页面加载都重置，不做持久化。**

这是一个有意的安全取舍：持久化的代价是，操作者上次离开时切到了晚报，下次进来在没注意的情况下把一整批新闻采纳进了晚报，而且不会有任何报错。这类错误事后很难发现。每次重置成综报，最坏情况是多点一下。

### 4.2 切换时哪些区域需要刷新

| 区域 | 切换时 |
|---|---|
| 候选列表（待筛选） | **不刷新，内容不变** |
| 丢弃列表 | **不刷新，内容不变** |
| 采纳列表 | **重新拉取** |
| 备选列表 | **重新拉取** |
| 计数徽标 | **重新拉取** |

候选列表不刷新这一点请特别注意：切换报别时如果顺手把整页数据都重拉一遍，操作者正在筛选的位置、滚动位置、已勾选但尚未提交的选择都会丢失。这是很影响体验的问题。**切换报别不应该干扰正在进行的筛选工作。**

### 4.3 计数徽标

`GET /stats` 返回五个数字。切换报别后：

- `pending`、`discarded` 两个数**不会变化**（后端已保证）
- `selected`、`backup` 两个数会随报别变化

响应里还会带一个 `exported` 字段。界面上不展示已导出的统计数字，忽略该字段即可，**不要因为它存在就在界面上加回来。**

如果界面上采纳/备选的计数徽标与其列表并排显示，两者必须严格一致——**数字写着 24 而列表只有 3 条，是这个功能最容易出的错，也是最影响信任的错。**

### 4.4 操作反馈

采纳/备选成功后的提示信息，建议带上目标报别（例如"已采纳到晚报"）。当前处于非默认报别（晚报）时，这个确认能显著降低误操作。

---

## 五、必须遵守的既有约定

仓库的 `src/console/AGENTS.md` 里有几条与本任务直接相关：

- **不要单独重命名** `web_static/js/manual_filter/*` 依赖的 DOM id、`data-*` 属性、CSS class 或 API path。模板和 JS 通过这些紧密耦合，改动必须成对进行。
- 沿用现有的 JS 模块边界：filter、review、discard、search drawer 各管各的。切换控件的状态应该有一个明确的归属模块（`core.js` 或 `workspace.js` 是合理候选），**不要在多个模块里各存一份当前报别**。
- 避免多个模块重复发起同类 API 请求。
- CSS 遵循现有拆分：页面专属样式放 `css/modules/filter.css`，不要在模板里写 inline style。

---

## 六、验收标准

1. 切换到晚报，采纳一条新闻；该条出现在晚报的采纳列表，不出现在综报的采纳列表。
2. 反复切换报别，**候选列表的内容、滚动位置、已勾选未提交的选择全部保持不变**。
3. 反复切换报别，丢弃列表的内容保持不变。
4. 计数徽标中的采纳 / 备选数字，与切换到该报别后列表里的实际条数严格一致。
5. 切换报别时，待筛选和已丢弃的计数不发生变化。
6. 在晚报模式下编辑一条待筛选新闻的摘要并保存，该条不得从候选列表中消失。
7. 在晚报模式下把一条晚报采纳的新闻退回待定，它重新出现在候选列表中。
8. 页面刷新后，切换控件回到"综报"。
9. `/duty` 路径（值班编辑视图）的现有功能不受影响。
