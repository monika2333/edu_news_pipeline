# 报送存档与查重功能 · 功能说明

面向：业务负责人、实现方
状态：已实现并启用（2026-07-30）

---

## 一、要解决什么问题

我们每天向上级报送教育舆情。同一件事，媒体往往会连续报道好几天。今天报过的事，明天、后天还会有新的稿子冒出来。

现在的系统认不出这种情况。系统只知道"这条链接我导出过"，但换一家媒体重发同一件事，链接变了、编号变了，系统就当成新新闻推给编辑。结果是编辑每天要在已经报送过的事情上反复做判断。

**这个功能要做的事**：把我们已经报送过的内容记下来，形成一个"已报送库"。每天新闻进来，系统自动比对一遍，凡是和已报送内容重复的，打上「已报送」标记。编辑就可以只看确定不重复的那些，标记过的快速扫一眼、大部分直接放弃。

---

## 二、几个说法的约定

为了后面表述清楚，先约定几个词：

| 说法 | 指什么 |
|---|---|
| **报告** | 一份完整文档，就是我们平时报送的那种 |
| **条目** | 报告里的一条新闻，比如综报里带 ★ 的一段 |
| **已报送库** | 所有报告拆开后，全部条目汇总成的库 |
| **回链** | 把一个存档条目对应回系统里的原始新闻 |
| **查重** | 拿今天的新闻去和已报送库比对 |

报告分三种：

- **综报**：《首都教育每日舆情综报》，当天整理当天报送
- **晚报**：《首都教育舆情》，前一天晚上整理、第二天早上报送，所以文档上印的是第二天的日期
- **反馈**：对方单位的定稿。里面既有我们报送后被采用的，也有对方自己补充的

---

## 三、核心业务规则

这一节是整个功能的骨架，实现时不能改。

### 1. 三种报告完全互斥

只要一条新闻在这三种报告的**任何一份**里出现过，以后就不再报送。不区分是综报还是晚报，也不管是我们报的还是对方补的——只要见过刊，就是已报送。

所以比对的时候不分类型，三种报告的条目混在一起当成一个库。报告类型只用来显示（比如告诉编辑"这条 7 月 26 日的晚报报过"），不影响判断。

### 2. 对方补充的条目也算已报送

反馈里有些条目是我们没报过、对方自己加进去的。这些同样进已报送库——它已经见刊了，我们再报就是重复。

### 3. 比对范围是最近 15 天

只拿最近 15 天的报送记录去比对今天的新闻。

理由：一件事的媒体跟进热度通常几天就衰减完了，15 天留了充足余量。同时这个范围足够短，不会撞上"每年同期都会有"的新闻——比如去年 7 月的中考录取和今年 7 月的中考录取，标题内容都很像，但显然不是重复。

**注意**：15 天是"比对范围"，不是"保留范围"。已报送库里的东西永久保留，随时可以手工查。如果哪天想把范围改成 20 天或 10 天，改个设置就行，不需要重新整理任何数据。

超过 15 天还在被讨论的事（比如重要政策文件的持续解读），由人工判断，系统不管。为此提供一个不限日期的全库搜索功能（见第四节第 4 条）。

### 4. 判定为重复的新闻不删除，只标记

系统判定重复的新闻照常显示在待审列表里，只是多一个「已报送」标记。编辑自己决定要不要看。

原因是系统会判错。真删了，编辑连翻案的机会都没有。

### 5. 编辑可以推翻系统的判断

标记旁边有「不是重复」按钮。点了之后标记消失，这条新闻恢复正常。

这些推翻记录会被留下来。跑一段时间后，可以用它们来看系统判得准不准，从而调整判定的松紧。

---

## 四、编辑怎么用这个功能

### 1. 录入报告

每天（或攒几天）把报告文本粘贴进系统。

具体操作：

1. 打开录入页面，把一份报告的完整文本粘贴进去
2. 点「解析」，系统自动拆出每一条新闻——标题、正文、来源、链接
3. 屏幕上显示拆解结果，可以逐条检查、修改、删除，也可以手工补一条
4. 确认无误后保存

一次粘贴一份报告，三种类型分开贴。

系统会自动判断这是哪种报告（综报有"综报"字样，晚报有期号，反馈没有期号），预先选好类型。如果你手选的和系统判断的不一致，会给一个提醒，但不阻止你继续——万一格式有变化，以你选的为准。

**关于日期**：文档上印的日期系统直接读。另外有一个"实际整理日期"，系统会预填（综报同日，晚报和反馈往前推一天），你可以改。

为什么要多这一个：反馈的间隔不固定。这次是隔一天中午收到，碰上节假日可能隔三天。这个信息只有你在录入的当下知道，事后从文档上是看不出来的，所以趁着录的时候记一下。这个字段现在不参与任何计算，纯粹是留着以后用。

### 2. 处理待确认的回链

保存报告后，系统会自动去比对：这些条目对应系统里的哪条原始新闻？

结果分三种：

- **确定是同一条**：系统自动关联，不用你管
- **拿不准**：进"待确认列表"，等你有空处理
- **系统里根本没有**：说明这条新闻我们的数据源没抓到，标记为"未覆盖"

待确认列表是**独立的一个页面，不卡在录入流程里**。录一份报告如果要点十几次确认才能保存，就没人愿意录了，久而久之这个库就废了。所以录完就是录完了，确认的事什么时候有空什么时候做。

确认页面左右对照：左边是报告里的条目，右边是系统里最像的那条新闻。你判断是不是同一条，点「是」或「不是」。

### 3. 日常审稿时看标记

待审列表里的新闻，如果和已报送库里的内容像，会显示「已报送」标记。

标记分两种深浅：

- **疑似已报送**：系统判断的，还没人确认过
- **已报送**：有人确认过的

鼠标放上去，会显示是和哪一天、哪份报告的哪条撞了，以及像到什么程度。

列表上方有个「隐藏已报送」开关，打开就只看没标记的新闻。默认是关的。

如果标记错了，点「不是重复」，标记消失。

### 4. 全库搜索

单独一个搜索页，输关键词能搜到已报送库里所有相关条目，**不受 15 天限制**。

用途：遇到跨度长的持续性事件，你想确认"这事我们以前到底报过没有"，来这里搜一下。

---

## 五、系统怎么判断"是不是重复"

这一节说的是判断逻辑，具体技术手段由实现方决定。

### 两件不同的事

功能里有两处在做"匹配"，但性质完全不同，容易混：

|  | **回链** | **查重** |
|---|---|---|
| 在比什么 | 报告里的条目 ↔ 系统里的原始新闻 | 今天的新闻 ↔ 已报送库 |
| 两边文字的关系 | 几乎一模一样，或者完全没关系 | 说的是同一件事，但措辞完全不同 |
| 什么时候跑 | 录入报告时跑一次 | 每天自动跑 |

**回链失败不影响查重。** 一个条目哪怕完全对不上系统里的任何新闻，只要它有标题和正文，照样能参与查重。所以录入的时候不要因为回链结果不好就觉得录错了。

### 回链的逻辑

报告里的条目，来源分三类：

1. **和系统导出的一模一样**——这类应该全部自动关联上
2. **有微调**——比如标题删了个感叹号、正文改了几个字。这类大部分也能自动关联上；改动大的会进待确认列表
3. **压根不是系统里的新闻**——对方自己加的，或者我们数据源没抓到的。这类关联不上，标记为"未覆盖"

判断方式是**比对文字的重合程度**，标题和正文各算一个分，标题权重高一些。

- 重合度很高（初步定 85% 以上）→ 自动关联
- 重合度中等（55% 到 85% 之间）→ 进待确认列表
- 重合度很低（55% 以下）→ 判定为未覆盖

**这三个数字是拍脑袋定的经验值，不是实测结果。** 之所以把下限放到 55%（而不是更高），是因为：定高了，真正能对上的条目会被静默丢掉，你根本不会知道；定低了，只是多几条明显不像的进待确认列表，扫一眼就能否掉，代价很小。

**一个重要要求**：哪怕判定为"未覆盖"，系统也要把当时算出来的分数和最像的那条候选记下来。

为什么：录完两周存量之后，我们就能看到全部条目的分数分布——真正对上的集中在什么区间、对不上的最高能到多少。有这个数据，85% 和 55% 这两条线该往哪边挪就一目了然。没有这个数据，两周之后手上还是只有拍脑袋的数字。这个信息在跑的时候记下来不花什么成本，事后想补就得全部重跑一遍。

### 查重的逻辑

查重面对的是完全不同的难题。举个真实例子：

7 月 27 日发布了《残疾人保障和发展"十五五"规划》，我们晚报里出了两条：

- 未来五年将深入开展孤独症全生命周期关爱促进行动
- 中国残联：将健全适龄残疾儿童入学评估机制，完善送教上门

今天媒体跟进的稿子标题可能是《〈残疾人保障和发展"十五五"规划〉印发：特殊教育向学前和高中阶段延伸》。

**这三个标题字面上几乎没有重合。** 所以查重不能靠比对文字——必须靠**理解语义**，判断"说的是不是同一件事"。

系统已经有现成的语义比对能力（现在用来把当天相似的新闻归类），直接复用就行，不需要额外的技术投入，也不产生额外的调用费用。

判定逻辑：

1. 每个存档条目入库时，算一次"语义指纹"
2. 每天新闻进来，也算语义指纹
3. 拿今天的新闻，和最近 15 天的存档条目逐一比，找出语义最接近的几条
4. 相似程度超过设定线的，标记为「疑似已报送」

同样，这条线的初始值也是经验值，跑一段时间后根据编辑点「不是重复」的频率调整。

**一条新闻可能同时和多条存档条目重复**（比如上面那个例子，跟进稿会同时撞上晚报的两条）。系统要能记录多个匹配，显示的时候只显示日期最早的那一条，其余折叠。

---

## 六、系统要记住哪些信息

不涉及具体存储方式，只说需要记住什么。

### 报告层面

每份录入的报告要记：类型、文档上印的日期、实际整理日期、期号、原始粘贴的文本、录入时间。

**为什么要留原始粘贴的文本**：如果以后发现拆解规则有问题（比如某种格式没考虑到），可以拿它重新拆一遍，不需要你再去翻文件重新粘贴。拆开后的条目是拼不回原文的，标题行、期号、格式标记这些都丢了。

不记"谁录的"——只有一个人在录，记了也没意义。

### 条目层面

每条要记：属于哪份报告、在哪个章节、第几条、标题、正文、来源、链接、语义指纹，以及回链的结果（关联到哪条新闻、当时算的分数、状态是自动关联还是人工确认还是未覆盖、谁确认的）。

**同一条新闻在库里可能出现两次**——我们自己报过、对方反馈里又出现了。这是正常的，不要合并。合并了以后就没法分析"我们报的东西哪些被采用了"。显示的时候去重就行。

### 匹配结果

每一对"今天的新闻 ↔ 存档条目"的匹配要记：相似度、怎么判出来的、当前状态（疑似 / 已确认 / 已否决）、谁否决的、什么时候。

**这些信息为什么要单独记，而不是挂在新闻上**：相似度、是谁否决的这些，既不属于那条新闻，也不属于那个存档条目，而是属于"这一对配对关系"。而且一条新闻可能配上多个存档条目，挂在新闻上就只能存一个。

---

## 七、明确不做的事

以下都讨论过，明确排除在这一版之外：

| 不做 | 原因 |
|---|---|
| 用大模型来判断两条是不是同一件事 | 语义比对的效果如果够用就不需要。留作后续备选，先跑数据看 |
| 导入 Excel / CSV / 上传文件 | 存档就是文本，一份一份粘贴就够，先不做多余的入口 |
| 采纳率统计（我们报了但对方没用） | 对方不采用的原因很多，参考价值不大，暂不关注 |
| "对方有、我们没报"的差集分析 | 这个有价值（能发现数据源缺口和评分漏判），但属于后续规划，不放进这一版 |
| 为长周期政策文件单开更长的比对窗口 | 属于优化。先用统一的 15 天跑出真实数据再说 |

---

## 八、分两期做

### 第一期：把库建起来

做录入功能、拆解功能、回链、待确认列表、全库搜索。

**这一期做完就先用起来**，把最近两周的报告贴进去。

**验收标准**：能顺利把三种报告贴进去、拆解正确；回链能自动关联上大部分条目；拿不准的能在待确认列表里逐条处理；能搜到库里的内容。

这一期结束后，看一下回链分数的分布情况，为第二期定判定线。

### 第二期：查重跑起来

做语义指纹、每日自动比对、已报送标记、隐藏开关、「不是重复」按钮。

**验收标准**：今天新闻里确实和近 15 天报送重复的，能被标出来；标错了能一键去掉；开启「隐藏已报送」后，编辑要看的量有明显下降。

---

## 九、需要提前知道的几件事

**历史报告的回链命中率会偏低。** 系统上线之前的报告，对应的原始新闻在系统里根本不存在，肯定关联不上。这是正常的，不是程序出错。

**判定线是要调的。** 文档里所有百分比都是初始值。跑两周之后拿真实数据校准一次，这是计划内的事，不是返工。

**录入的顺畅程度决定这个功能的成败。** 如果录入很麻烦，库就攒不起来，后面所有功能都是空的。所以实现时凡是在"少点几下鼠标"和"多存一点信息"之间做取舍的，优先选前者。
