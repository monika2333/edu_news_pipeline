# 新闻评分反馈功能实施方案（v12）

## 一、目标

在控制台新闻卡片的分数旁增加评分反馈入口，提供两个反馈选项：

- **偏高**：操作员认为当前展示的评分高于新闻实际重要性。
- **偏低**：操作员认为当前展示的评分低于新闻实际重要性。

选择方向后，录入员可**可选地**填写一段简短理由（解释为什么觉得偏高/偏低），作为未来调整提示词的参考。认为评分合适时无需操作；不单独保存"合适"状态。

反馈数据以结构化方式保存在数据库中，供后续调整和回归验证重要性评分提示词使用。核心诉求是：**最终能知道"由哪套提示词的哪一版，某篇新闻被判为偏高还是偏低，以及录入员给出的理由"**。本功能只收集反馈，**不自动修改提示词，也不自动改动生产评分**。

## 二、设计原则

1. 评分反馈与"采纳、备选、放弃"等人工筛选决定相互独立，单独存表，不在 `manual_reviews` 中增加不同职责的字段。
2. **反馈以"一篇新闻 ×（一套提示词的某个版本）"为单位，保留一条当前有效反馈。** 同一（套 × 版本）内从"偏高"改"偏低"是**覆盖同一行**（该粒度内的犹豫轨迹无价值，不保留）；换了版本、或（因类别变化）换了套，则**新开一行**，使各套各版本下的判断各自留存、可精确回溯。
3. 保存反馈时由服务端根据 `article_id` 读取评分上下文（分数、prompt_key、提示词版本）做快照，且这几项来自**同一条外部重要性评分记录**、彼此一致。
4. 反馈只针对页面展示的 `external_importance_score`，并保存提交反馈时的评分值快照。
5. 反馈不区分综报和晚报。评分属于新闻本身，同一条新闻不因 `report_type` 不同而区别对待。
6. **备注是反馈的附属信息**：只在已选择偏高/偏低时可填，随反馈行一同保存；撤销反馈时一并消失。

## 三、前置准备

### 3.1 提示词版本管理（本方案随附建立）

当前提示词无版本号。为让每条外部重要性评分可回溯到"哪套提示词的哪一版"，本方案随附引入一套轻量版本管理，核心原则是**提示词文件名保持稳定、代码不因升级而改动**：

- **文件名不变。** `config/prompts/` 下现有的几个 `.md` 文件名保持稳定，加载它们的代码永远指向同一路径。升级提示词 = 直接改这些 `.md` 文件的**内容**，绝不改名。
- **每套提示词各自独立版本号。** 在 `config/prompts/` 下新增一个版本文件（如 `VERSIONS`），一套一行，记录四套各自的当前版本，例如：

  ```
  external_positive: v1
  external_negative: v1
  internal_positive: v1
  internal_negative: v1
  ```

  改了哪套提示词，就**只把那一行的版本递增**，与该套内容在**同一次提交**里一起变更；没动的那三套版本号不变、其历史反馈继续沿用。
- **精确定位靠"套 + 版本"。** "该条评分实际用的那套 `prompt_key` + 那套的版本号"两者相合，即可精确定位是哪套的第几版打的分——正好支撑"分开迭代四套提示词"的目标。
- **打分时把版本与 prompt_key 写进 `external_importance_raw`。** 外部重要性评分成功、写入 `external_importance_raw` 的那一步（现已写有 `category`、`model_output`、`decided_at`），把 `prompt_key` 与**该套（即 `prompt_key` 对应那套）的版本号**一并写入同一个 jsonb。这样"这条重要性分是哪套提示词、哪一版打的、候选类别、分数"全部待在一起、彼此自洽。**不写入 `score_details`**——那是教育相关性评分的明细，与本功能无关。
- **版本号语义（按套各论各的）。** `external_negative` 的 `v2` 与 `internal_positive` 的 `v2` 是两码事。因此唯一键里带上 `prompt_key`（见第四节），既避免不同套同版本串的误撞，也让"按套 + 版本"成为反馈分析的自然维度。（若偏好版本串本身全局唯一、便于肉眼区分，也可给版本串带上套名前缀，如 `external_negative-v2`；有了三列唯一键，这只是可读性偏好、非必需。）
- **（可选，暂不实现）内容哈希兜底。** 手动改标签的天然弱点是：可能改了提示词却忘了抬版本号。若要免除这个负担，可在打分时顺手算一份该套提示词内容的哈希，与版本号一并存入 `external_importance_raw`。现阶段只上 `VERSIONS` 文件这一层即可，哈希日后可随时补。

### 3.2 类别（prompt_key）的来源——已确认

外部重要性评分选用哪套提示词，由代码内部的路由 key 决定，现已统一为与提示词文件一致的完整四值：`external_positive` / `external_negative` / `internal_positive` / `internal_negative`（见 `src/adapters/external_filter_model.py`）。反馈表的 `prompt_key` 就存这四值之一，由 5.1 在打分时写入 `external_importance_raw`，反馈保存时直接读取。

一点背景（供理解，实现无需额外处理）：评分先产生一个 6 值的**候选类别**（`external` / `external_positive` / `external_negative` / `internal` / `internal_positive` / `internal_negative`，写在 `external_importance_raw.category`），再归并到上述 4 值路由 key——不带正负后缀的 `external` / `internal` 归入对应的 `_positive`。因此 `prompt_key` 直接取路由 key 即可；原始 6 值候选仍留在 `external_importance_raw.category`，需要细看随时可 join。

| 候选 `category`（6 值） | `prompt_key`（4 值） |
|---|---|
| `external` / `external_positive` | `external_positive` |
| `external_negative` | `external_negative` |
| `internal` / `internal_positive` | `internal_positive` |
| `internal_negative` | `internal_negative` |

> **为什么 `prompt_key` 与 `prompt_version` 都必须非空：** 本方案用 `(article_id, prompt_key, prompt_version)` 作唯一键。Postgres 的唯一约束把 NULL 视为互不相同——只要其中任一列为空，同一篇新闻就可能冒出多条重复行、唯一约束形同虚设。因此两列都必须 `NOT NULL`。有了 3.1 的机制，所有新评分都在 `external_importance_raw` 里带着 `prompt_key` 与该套版本号，反馈直接读取。现阶段每套版本号是单一常量，唯一键等价于"一篇一行"；将来某套版本递增后，该套的新版本自动开新行、跨版本历史自动积累。

> **不含历史兼容逻辑。** 系统每天只处理新数据，改动上线前的旧记录已清除。因此无需为"老记录缺版本/prompt_key"编写现场归并或常量兜底——所有进入反馈流程的新闻都必然带着版本与 prompt_key（它们与 `external_importance_score` 在同一步写入，且仅在通过外部筛选、入队人工审核时发生）。

## 四、数据模型

新增 Dbmate migration，创建 `score_feedbacks` 表。建议字段如下：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `uuid` | 主键，默认 `gen_random_uuid()` |
| `article_id` | `text` | 关联 `news_summaries(article_id)`，删除新闻时级联删除 |
| `feedback_type` | `text` | 只能是 `too_high` 或 `too_low` |
| `score_value` | `numeric(6,3)` | 提交反馈时 `external_importance_score` 的服务端快照 |
| `prompt_key` | `text` | **`NOT NULL`。** 实际打分用的那套提示词：`external_positive` / `external_negative` / `internal_positive` / `internal_negative`（取自 `external_importance_raw`，见 3.2） |
| `prompt_version` | `text` | **`NOT NULL`。** 产生该分数的那套提示词的版本标签（按套各自编号），取自 `external_importance_raw`（见 3.1） |
| `notes` | `text` | 录入员可选填的理由说明，随反馈行一同保存（界面已启用，见 6.1） |
| `score_context` | `jsonb` | 可选评分上下文（分数明细、模型输出等） |
| `created_at` | `timestamptz` | 首次反馈时间 |
| `updated_at` | `timestamptz` | 最近修改时间（同（套×版本）内切换偏高／偏低、或编辑备注时更新） |

约束和索引：

- **`UNIQUE (article_id, prompt_key, prompt_version)`**：保证一篇新闻在"某套提示词的某个版本"下只有一条当前有效反馈；换套或换版本则另起一行。
- `prompt_key` 与 `prompt_version` 均声明为 `NOT NULL`（原因见 3.2）。
- `CHECK (feedback_type IN ('too_high', 'too_low'))`。
- `CHECK (prompt_key IN ('external_positive', 'external_negative', 'internal_positive', 'internal_negative'))`。
- 建议对 `notes` 长度设上限（如 500 字），后端与前端各挡一道。

### 4.1 当前反馈状态如何读取

新闻列表查询时，读取每篇在**当前评分所用的 `prompt_key` + 该套版本**下的那一行（`article_id` + 当前 `prompt_key` + 当前 `prompt_version`）：据此决定触发按钮显示灰色问号（未反馈）、▲（偏高）还是 ▼（偏低）；浮窗打开时再据 `feedback_type` 高亮对应方向、把 `notes` 回填到输入框。由于现阶段各套版本号是常量，这等价于"一篇取一行"。

## 五、后端实现

### 5.1 打分步骤补记版本与 prompt_key（前置改动）

在外部重要性评分成功、写入 `external_importance_raw` 的那处（`src/adapters/db_postgres_process.py` 258 附近，与现有 `category`/`model_output`/`decided_at` 同一写入点）：把该条评分实际选用的 **`prompt_key`**（即代码内部路由 key，已是完整四值之一）与**该套对应的版本号**（从 `config/prompts/VERSIONS` 里取 `prompt_key` 那一行）一并写入 `external_importance_raw`。

此改动是纯附加：不改变任何既有评分逻辑，只在结果 jsonb 里多记两个字段。完成后，后续所有新评分都自带 prompt_key 与该套版本号。

### 5.2 数据库 adapter

新增 `src/adapters/db_postgres_score_feedback.py`，由 `PostgresAdapter` 暴露：

- `upsert_score_feedback(...)`：校验文章存在，读取当前外部重要性评分上下文（`external_importance_score`、以及 `external_importance_raw` 里的 `prompt_key` 与该套版本），按 `(article_id, prompt_key, prompt_version)` 新增或更新反馈。**同一次 upsert 同时写入 `feedback_type` 与可选的 `notes`**——因此"改方向"和"改备注"复用同一条写入路径。
- `clear_score_feedback(...)`：删除该文章在当前 `prompt_key` + `prompt_version` 下的反馈行（备注随之消失）。

所有 SQL 使用参数化查询。upsert 应在数据库事务内完成，避免读取分数与保存快照之间出现不一致。

### 5.3 控制台 service

新增 `score_feedback_service.py`，负责：

- 标准化和校验 `article_id`、`feedback_type`、备注（含长度上限）。
- 调用 adapter 保存或撤销反馈。
- 将不存在的文章、超长备注等转换为明确的业务错误。
- 为新闻列表返回当前反馈状态（含 `notes`），供触发按钮状态与浮窗回显。

人工筛选 service 不直接编写反馈 SQL，也不把反馈逻辑混入采纳／备选／放弃流程。

### 5.4 API

在现有 `/api/manual_filter` router 下新增：

```text
PUT  /api/manual_filter/score-feedback
POST /api/manual_filter/score-feedback/clear
```

保存请求示例（`notes` 可选；无论是点方向按钮、还是失焦保存备注，都走这个 upsert 接口）：

```json
{
  "article_id": "article-123",
  "feedback_type": "too_high",
  "notes": "该主体近期已多次报道，重要性被高估"
}
```

- 撤销请求只需 `article_id`（服务端按该文章当前的 `prompt_key` 与 `prompt_version` 定位要删除的行）。
- `article_id` 走请求体，**不放进 URL path**（项目中部分文章 ID 含 `/`）。
- 保存接口按 `(article_id, prompt_key, prompt_version)` upsert：重复点击同一选项不产生重复数据；从"偏高"切到"偏低"更新原行与评分快照；仅编辑备注时携带当前 `feedback_type` 一同 upsert；换套或换版本则自然落到新行。

### 5.5 新闻列表返回值

人工筛选查询关联 `score_feedbacks`（`article_id` + 当前 `prompt_key` + 当前 `prompt_version`），在每条新闻中返回：

```json
{
  "score_feedback": {
    "feedback_type": "too_high",
    "score_value": 82,
    "notes": "该主体近期已多次报道，重要性被高估",
    "updated_at": "2026-07-21T10:00:00Z"
  }
}
```

没有反馈时返回 `null`。序列化逻辑只整理 API 字段，不负责数据库查询。

## 六、前端交互

### 6.1 触发按钮与浮窗

卡片上分数旁只放一个紧凑的**触发按钮**，点击后弹出浮窗，偏高/偏低与备注都收在浮窗里，避免列表拥挤。触发按钮的外观直接反映当前反馈状态：

- **未反馈**：低调的**灰色问号**（不显眼，不与采纳/放弃等主操作抢视觉层级）。
- **偏高**：**▲**。
- **偏低**：**▼**。

（方向映射：`too_high` → ▲，`too_low` → ▼。）

```text
卡片（未反馈）：   分数：82  ⌾（灰色问号）
卡片（已反馈偏高）： 分数：82  ▲

浮窗（点开后）：
┌─ 评分反馈 ────────────────┐
│ 这条分数： [偏高] [偏低]     │
│ 备注（选填）：              │
│ [ 说明你判断偏高/偏低的理由… ] │
└──────────────────────────┘
```

**触发按钮规则：**

1. 触发按钮外观由列表返回的 `score_feedback` 决定，三态如上：未反馈=灰色问号、偏高=▲、偏低=▼。
2. 点击触发按钮打开浮窗；点击浮窗外部或按 `Esc` 关闭；编辑备注期间保持打开。
3. 打开浮窗时回填当前状态：高亮已选方向、预填已存备注（无反馈则两个方向都不高亮、备注框置灰）。
4. **关闭浮窗时，若备注有未提交的改动，视同一次失焦、先提交再关闭**，避免丢失刚打的字。

**方向按钮规则（浮窗内）：**

5. 点击"偏高"或"偏低"→ 调用 upsert 接口 → 成功后高亮对应按钮、启用备注输入框；触发按钮变为对应箭头（▲ / ▼）。
6. 点击另一个选项 → 直接切换反馈（更新同一行），**并清空备注**（避免理由与方向对不上）；触发按钮随之更新箭头方向。
7. 再次点击已选中的选项 → 调用撤销接口，恢复未选择状态，备注随之清空、输入框置灰；触发按钮变回灰色问号。
8. 请求期间禁用两个方向按钮，避免快速重复提交。
9. 保存失败时恢复原状态并显示错误 toast。
10. 反馈操作不会移除新闻卡片，也不会改变采纳／备选／放弃状态。

**备注输入框规则（沿用现有摘要编辑的失焦保存手感）：**

11. 备注框仅在已选择偏高/偏低时可用；未选方向时置灰。
12. **保存时机与摘要一致：绑定 `change` 事件**（失焦且内容有改动时才触发），不在每次按键（`input`）时保存；浮窗关闭也触发一次提交（见规则 4）。`input` 事件仅用于字数统计、输入框高度自适应等界面刷新，不触发保存。
13. 触发保存时，调用评分反馈的 **upsert 接口**（非摘要用的 `/edit`），请求体带上当前 `feedback_type` 与 `notes`。成功后 toast「备注已保存」，失败 toast「备注保存失败」——与现有摘要/来源保存的提示保持一致。
14. 备注长度上限（如 500 字），前端限制输入并给出提示，后端二次校验。

建议抽取统一的触发按钮 + 浮窗控件（含方向按钮与备注框）渲染与事件处理函数，供筛选页、复核页、废弃页复用，避免三处各自维护状态。

### 6.2 可访问性与样式

- 触发按钮使用真实的 `<button type="button">`，并配能反映当前状态的 `aria-label`（如未反馈"评分反馈"、已反馈"评分反馈：偏高"）——不要只靠 ❓/▲/▼ 字符本身传达含义；用 `aria-haspopup` / `aria-expanded` 表达浮窗开合状态。
- 触发按钮的三态除箭头形状外，不只靠颜色区分（▲/▼ 形状本身已表意，符合此要求）。
- 浮窗打开时管理焦点（焦点移入浮窗，`Esc` 关闭并把焦点还给触发按钮）。
- 浮窗内两个方向按钮使用真实的 `<button type="button">`，用 `aria-pressed` 表达选中状态；选中时同时增加边框、背景和文字状态，不只靠颜色。
- "偏高""偏低"用不同但不过度醒目的颜色，避免抢占采纳／放弃操作的视觉层级。
- 备注输入框在禁用态有明确的视觉区分（置灰 + `disabled`），并配合适当的占位提示文字。

## 七、实施顺序

1. **建立提示词版本管理：** 新增 `config/prompts/VERSIONS`（四套各一行）；改外部重要性评分写入点，把 prompt_key 与该套版本号写进 `external_importance_raw`（见 5.1）。
2. 新增数据库 migration 与评分反馈 adapter。
3. 增加 service、Pydantic 请求／响应模型与 API（含 `notes` 字段与长度校验）。
4. 扩展人工筛选查询与 serializer，返回当前反馈（含 `notes`）。
5. 在筛选页新闻卡片加入触发按钮 + 浮窗控件与事件处理。
6. 将同一控件复用到复核页与废弃页。
7. 补齐测试并运行相关测试集。

## 八、测试计划

### 提示词版本与 prompt_key

- 外部重要性评分成功后，`external_importance_raw` 中带有 `prompt_key` 与该套在 `VERSIONS` 中的当前版本号。
- 只递增某一套的版本后，该套新评分记录到新版本、其余三套记录的版本号不变。
- 候选类别归并到 4 值 `prompt_key` 的映射正确（含不带后缀的 `external`/`internal` 归入对应 `_positive`）。

### 数据库 adapter

- 首次提交反馈能新增记录。
- 同一 `(article_id, prompt_key, prompt_version)` 重复提交时更新原记录，不产生重复行。
- "偏高"能切换为"偏低"（更新同一行）。
- **同套换版本、或（因类别变化）换套，各自独立成行、互不覆盖**；尤其验证**同一篇文章、两套不同 prompt_key 但版本串相同（如都为 `v1`）时不误撞**（这正是三列唯一键要解决的场景）。
- 保存的 `score_value` 与新闻的 `external_importance_score` 一致。
- `prompt_key` 为四值之一、`prompt_version` 为对应套的版本，二者与 `external_importance_raw` 一致且均非空。
- **仅编辑备注的 upsert 只更新 `notes`、不改变 `feedback_type`；备注正确落库。**
- 撤销反馈后该 `(article_id, prompt_key, prompt_version)` 行被删除（备注一并消失）。
- 不存在的 `article_id` 返回明确错误。

### Service 和 API

- 只接受 `too_high`、`too_low`。
- 空 `article_id`、**超长备注**等非法输入被拒绝。
- 保存、切换、撤销接口返回一致的响应结构。
- 控制台认证规则继续生效。

### 前端

- **触发按钮三态正确**：未反馈显示灰色问号、偏高显示 ▲、偏低显示 ▼（由列表返回值决定）。
- **点击触发按钮打开浮窗，点击外部或 `Esc` 关闭。**
- **打开浮窗时正确回填已选方向与已存备注。**
- **关闭浮窗时，未提交的备注改动被提交保存。**
- 保存成功后更新方向按钮与触发按钮箭头。
- 切换与撤销行为正确（切换更新箭头方向，撤销回到灰色问号）。
- **备注框在未选方向时禁用；选择方向后可编辑。**
- **备注在失焦（`change`）且有改动时保存，逐字输入（`input`）不触发保存。**
- **切换方向时备注被清空。**
- 请求失败时恢复原状态并提示错误。
- 反馈操作不会触发新闻筛选状态的 change handler。
- 聚类卡片和普通卡片都能独立提交反馈。

建议至少运行：

```bash
python -m pytest tests/test_manual_filter_service.py
python -m pytest tests/test_manual_filter_routes.py
python -m pytest tests/test_db_postgres_manual_reviews.py
python -m pytest tests/test_console_web_routes.py
```

评分反馈 adapter 建议增加独立测试文件 `tests/test_db_postgres_score_feedback.py`。

## 九、验收标准

- 外部重要性评分携带 `prompt_key` 与该套当前版本号（写入 `external_importance_raw`）。
- 操作员能通过分数旁的触发按钮打开浮窗，提交"偏高"或"偏低"反馈；认为合适时无需操作。
- 触发按钮能反映该新闻的反馈状态：未反馈=灰色问号、偏高=▲、偏低=▼。
- 选择方向后可选填备注；备注在页面重新加载后正确回显。
- 已有反馈在页面重新加载后仍正确显示。
- 操作员可以切换或撤销反馈（切换清空备注，撤销删除整行）。
- 同一（套 × 版本）下反复改动只保留最终判断；**不同套、或不同版本下的反馈各自留存、可精确回溯**。
- 反馈不会改变新闻的人工筛选状态。
- 每条反馈都保存服务端确认的评分值、`prompt_key`、该套版本号快照，以及录入员填写的备注。
- 所有新增 API 受现有控制台认证保护，相关自动化测试通过。

## 十、未来扩展（暂不实现）

- **提示词内容哈希：** 见 3.1，作为忘记抬版本号时的兜底。
- **区分操作员：** 若将来需要区分不同操作员的反馈，可新增 `actor` 列；如需让不同操作员在同一（套 × 版本）下各留一条，再把 `actor` 一并加入唯一键（`(article_id, prompt_key, prompt_version, actor)`）。当前单人使用，无需处理。
