# 筛选页报别切换 —— 后端实施任务

**仓库分支基线**：`codex/submission-archive`
**本任务只做后端。前端由另一位实施者负责，见配套文档。**

---

## 零、路径白名单

**允许修改：**

```
src/console/manual_filter_query_service.py
src/console/manual_filter_admin_service.py
src/console/manual_filter_action_service.py
src/console/manual_filter_helpers.py
src/console/manual_filter_routes.py        （仅在必要时）
src/adapters/db_postgres_manual_reviews.py
tests/
```

**严禁修改：**

```
src/console/web_templates/          ← 全部
src/console/web_static/             ← 全部（HTML / JS / CSS 一律不要动）
```

界面由另一位实施者实现。本任务交付的是可以用 curl 独立验证的 API 行为。如果你觉得某个界面行为必须靠改模板才能实现，**不要动手，在交付说明里写出来**。

---

## 一、要实现什么

筛选页要新增一个"综报 / 晚报"切换控件，决定"采纳"和"备选"把新闻归到哪个报。你不需要实现这个控件，但需要让后端正确响应它传来的值。

---

## 二、核心概念：`report_type` 只对"已归报"的状态有意义

`manual_reviews.status` 的五种取值与报别的关系不同：

| 状态 | 报别有意义吗 |
|---|---|
| `pending` 待筛选 | **没有**——还没归报 |
| `selected` 采纳 | **有** |
| `backup` 备选 | **有** |
| `exported` 已导出 | **有** |
| `discarded` 已丢弃 | **没有**——跟原本打算发哪个报无关 |

**待筛选和已丢弃是两个共用池子，不分报别；采纳、备选、已导出按报别区分。** 整个改动都从这一条推出来。

另外需要知道：`manual_reviews.report_type` 在这条链路里**目前是硬编码常量** `'zongbao'`（由 `manual_filter_serializers.py` 的 `FILTER_TAB_REPORT_TYPE` 统一提供），从未真正产生过第二种取值。本任务是第一次让它变成真实维度。**现有数据无需迁移。**

---

## 三、关键设计决定：读侧不过滤，而不是写侧强制值

要实现"待筛选和已丢弃不分报别"，有两条路：

- **写侧强制**：保证所有 pending / discarded 行的 `report_type` 都被写成 `'zongbao'`，读的时候继续按 `'zongbao'` 过滤。
- **读侧不过滤**：候选列表和丢弃列表的查询干脆不带 `report_type` 条件，行里存什么值都无所谓。

**必须采用后者。这是本任务最重要的一条要求。**

原因是失效模式的差别。写侧强制依赖"所有 pending 行的值必须是 zongbao"这个约定。一旦某处漏传参数、或退回待定时带上了切换值，那条新闻就会同时从候选列表和聚类结果里消失——而界面上没有任何入口能看到它。这是一个静默的数据黑洞。

读侧不过滤则让这种情况无害：哪怕真有一条 pending 行的值变成了 `'wanbao'`，它照样出现在候选列表里、照样参与聚类。**不是靠约定去防止坏状态，而是让坏状态失去杀伤力。**

**验收标准第 6 条专门检验这一点。如果你实现成了写侧强制，那条会通不过。**

### 数据库层已经支持"不过滤"

`db_postgres_manual_reviews.py` 里：

```python
def normalize_report_type_value(report_type):
    value = (report_type or "").strip().lower()
    if not value:
        return None          # 传 None 得到 None
```

而 `_build_manual_review_filters` 是 `if normalized_report_type:` 才追加过滤条件。**所以把 `None` 传到底层，SQL 里就不会出现 `report_type` 条件——不需要改任何 SQL。**

挡在中间的是 console 层的这个函数：

```python
def _normalize_report_type(report_type):
    value = (report_type or DEFAULT_REPORT_TYPE).strip().lower()   # None → 'zongbao'
    return value if value in VALID_REPORT_TYPES else DEFAULT_REPORT_TYPE
```

它永远不返回 `None`，所以服务层现在**没有办法表达"不分报别"**。需要让相关调用路径能把 `None` 传到底。怎么做由你决定（新增一个允许返回 `None` 的辅助函数、或在调用点直接绕过），但不要改动 `_normalize_report_type` 现有的行为，它还有其他调用方。

---

## 四、API 契约

以下是与前端约定好的接口行为。**前端会照这份契约发请求，请严格实现。**

| 端点 | 前端会传 `report_type` 吗 | 后端必须做什么 |
|---|---|---|
| `GET /api/manual_filter/candidates` | 不传 | **忽略该参数，不按报别过滤** |
| `GET /api/manual_filter/discarded` | 不传 | **忽略该参数，不按报别过滤** |
| `GET /api/manual_filter/review` | **传切换值** | 按该值过滤 |
| `GET /api/manual_filter/stats` | **传切换值** | 见第五节 |
| `POST /api/manual_filter/decide` | **传切换值** | 见下方说明 |
| `POST /api/manual_filter/order` | **传切换值** | 按该值确定排序作用域 |
| `POST /api/manual_filter/edit` | 不传 | **不写入 `report_type` 字段** |
| `POST /api/manual_filter/archive` | 不传 | **不写入 `report_type` 字段** |
| `POST /api/manual_filter/duplicate-check` | 传切换值 | 现有行为不变 |

所有端点的请求模型**已经**带有 `report_type` 字段（默认 `"zongbao"`），API 形状不需要改动，改的是各自的处理语义。为了兼容，即使前端不传，收到 `"zongbao"` 也应当按"忽略"处理——也就是说 `candidates`、`discarded`、`edit`、`archive` 这四个端点无论收到什么值都不使用它。

### `POST /decide` 需要特别注意

`BulkDecideRequest` 一次请求里可以同时带四组 id（`selected_ids` / `backup_ids` / `discarded_ids` / `pending_ids`），但只有一个 `report_type`。

`manual_filter_admin_service.bulk_decide()` 目前给四组 id 统一写入 `target_report_type`。必须改成：

- `selected_ids` / `backup_ids` → 写入切换值
- `discarded_ids` / `pending_ids` → **写入 `None`**，让 SQL 里现有的 `report_type = COALESCE(%s, report_type)` 保持原值不变

注意 `bulk_decide` 末尾调用 `update_manual_review_statuses_as_user(...)` 时也传了一个整体的 `report_type=target_report_type`（用于审计/事件记录）。那个参数的用途需要你确认后再决定是否保留原值。

### 写入方向不需要改 SQL

`report_type` 位于 `UPDATE` 的 `SET` 子句、而非 `WHERE` 子句（`WHERE` 只按 `article_id` 定位），所以写入 `'wanbao'` 能正确落库，不会出现"匹配不到行"的静默失败。

---

## 五、`manual_review_status_counts()` 需要改写

现在的实现是一条 `WHERE ... GROUP BY status`。整表级的 `WHERE` 对五个状态一视同仁，无法表达"只对其中三个过滤"。

改为用 Postgres 的 `FILTER` 子句，给每个聚合函数单独挂条件：

```sql
SELECT
  COUNT(*) FILTER (WHERE status = 'pending')   AS pending,
  COUNT(*) FILTER (WHERE status = 'discarded') AS discarded,
  COUNT(*) FILTER (WHERE status = 'selected'
                     AND COALESCE(report_type,'zongbao') = %s) AS selected,
  COUNT(*) FILTER (WHERE status = 'backup'
                     AND COALESCE(report_type,'zongbao') = %s) AS backup,
  COUNT(*) FILTER (WHERE status = 'exported'
                     AND COALESCE(report_type,'zongbao') = %s) AS exported
FROM manual_reviews
```

要点：

- 仍然是一条 SQL、一次全表扫描
- 整表级 `WHERE` 完全消失，原先的 `if normalized_report_type` 参数拼接分支可以删掉
- 返回值仍是同样五个键的字典，**调用方无需改动**
- 原先那段 `for row in cur.fetchall(): counts[status] = ...` 的动态赋值循环不再需要

**不要**简单地把旧实现的过滤去掉了事。那样 selected / backup 的计数会变成综报+晚报的合计，切到晚报时数字显示 24、列表只有 3 条。这是一个长期存在的错误信息，比缺功能更糟。

旁边的 `manual_review_pending_count()` 是独立函数，只数 pending，不需要改 SQL——调用时传 `None` 即可。

---

## 六、聚类

聚类的待聚类查询 `fetch_manual_pending_for_cluster()` 应改为**不按报别过滤**（传 `None`）。

理由与候选列表相同：pending 行不分报别，且这样能保证异常值不会让新闻从聚类中消失。

以下内容**不要改动**：

- 聚类只按四个桶划分（internal / external × positive / negative），与报别无关
- `cluster_id` 的生成规则 `f"{bucket_key}-{idx}"` 不含报别，这是正确的
- `manual_clusters` 表上的 `unique (cluster_id)` 约束**不要改**。该表永远只存在一种 `report_type` 取值（常量 `'zongbao'`），约束是安全的

---

## 七、下游不需要改动

`fetch_manual_selected_for_export()` 已经按 `report_type` 过滤取稿。采纳为 `'wanbao'` 的稿件会自动流入晚报导出。报送链路（值班编辑选稿、报送存档、管理员汇总）本来就以报别为真实维度，不需要任何配合改动。**不要为了这个功能去改报送链路的代码。**

---

## 八、验证

**验证由你完成，不是交给使用者去跑。** 前端实施者会直接依赖你交付的接口行为。具体测哪些用例、怎么组织，你自己判断——但有两件事需要先知道。

**一、`FakeAdapter` 不支持"不过滤"，必须先修它。**

`tests/test_manual_filter_service.py` 里的测试替身有这么一句：

```python
target_type = self._normalized_report_type(report_type)   # None → 'zongbao'
```

它把 `None` 归一成了 `'zongbao'`。也就是说你传 `None` 下去，替身仍然会按报别过滤——**你的测试会在错误的实现下照样通过。** 请让它在收到 `None` 时跳过过滤，与真实 adapter 行为一致，并检查该文件里其他接收 `report_type` 的 fake 方法是否有同样问题。

**二、单元测试碰不到真实 SQL。**

现有测试全部基于 fake adapter，第五节那段 `FILTER` 改写只有连上真实数据库才能验证。所以除了单元测试之外，需要起一个 console 实例做一轮实际接口验证。

如果执行环境连不上数据库，不要用 mock 糊过去——在交付说明里写明这一层未执行，并把需要人工跑的命令列出来。

交付时附上实际执行的输出（真实数字、真实响应片段），而不只是结论。

---

## 九、验收标准

以下是必须成立的事实。第 6 条是核心——它区分"读侧不过滤"（正确）与"写侧强制值"（错误）两种实现。

1. `GET /candidates` 的返回内容与传入的 `report_type` **完全无关**。
2. `GET /discarded` 的返回内容与传入的 `report_type` **完全无关**。
3. `GET /review?report_type=wanbao` 只返回归到晚报的采纳/备选稿件。
4. `GET /stats` 在两个报别下，`pending` 与 `discarded` 数值相同，`selected` / `backup` / `exported` 数值随报别变化，且与 `/review` 的实际条数严格一致。
5. `POST /decide` 在 `report_type=wanbao` 下同时提交 `selected_ids` 和 `pending_ids`：前者的 `report_type` 变为 `'wanbao'`，后者的 `report_type` **保持原值不变**。
6. **手动把某条 `status='pending'` 的行的 `report_type` 改成 `'wanbao'`（直接改数据库，模拟异常状态），它仍应正常出现在 `GET /candidates` 的结果中，并参与聚类。**
7. `POST /edit` 在 `report_type=wanbao` 下编辑一条 pending 稿件，该行的 `report_type` 不发生变化。
8. 采纳为晚报的稿件能被晚报导出正确取到。
9. 现有测试全部通过；报送链路、值班编辑、报送存档的行为无变化。
